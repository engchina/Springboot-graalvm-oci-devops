version: 0.1             
component: build
timeoutInSeconds: 5000
shell: bash
env:
  exportedVariables:
steps:
  - type: Command
    command: |
      docker login ${REGISTRY} -u ${DOCKER_USER} -p ${DOCKER_PWD}
      docker build -t ${IMAGE} .
      docker push ${IMAGE}